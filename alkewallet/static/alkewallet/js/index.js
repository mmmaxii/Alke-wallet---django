/* Solo dejamos animaciones o configuraciones si las hubiera,
   el manejo del login ahora está delegado al form nativo HTML y Django. */

// Eliminamos el e.preventDefault() original para que el formulario se envíe mediante POST real a la vista de Django.
